# Data

The puzzle inputs are stored in txt.gz files with filename format:

```text
<year>_<day>[_<part>].txt.gz
```

Where `_<part>` is optional, and used only when a multi-part puzzle
has different inputs for every part.
